# LDAP/OpenLDAP protocol names intentionally lead several compatibility
# diagnostics. Their capitalization is part of differential output, so ST1005
# is not a valid project-wide rule. DN is also pluralized as DNs; spelling that
# as DNS would incorrectly refer to the Domain Name System.
checks = ["all", "-ST1003", "-ST1005"]
